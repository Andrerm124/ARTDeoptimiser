<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/menu_show_sys_apps"
        android:title="@string/menu_sys_apps"
        android:checkable="true"
        app:showAsAction="never"/>

    <item
        android:id="@+id/menu_add_manual"
        android:title="@string/menu_add_manual"
        app:showAsAction="never"/>

    <item android:id="@+id/menu_show_legal"
          android:title="@string/menu_show_legal"
          app:showAsAction="never"/>
</menu>